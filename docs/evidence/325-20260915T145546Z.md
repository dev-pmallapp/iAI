---
issue: 325
story: 293
repo: dev-pmallapp/iAI
branch: task/325-milestone-corrections
commit: a3d02b09dc892e9bdf3c0456442c337cde76aecb
run: 2026-09-15T14:55:46Z
verdict: PASS
---

# Evidence: #325 — the `M2.md` and `M4.md` corrections, line-neutral

Build target 10 of `docs/design/stories/293.md`. Cases **3** and **19**.
The last task of S2.6.

`commit:` pins the **parent** of the commit carrying this artifact.

## Line-neutrality, measured

| File | Before | After |
|---|---|---|
| `docs/milestones/M2.md` | **261** | **261** |
| `docs/milestones/M4.md` | **259** | **259** |

Measured with `awk 'END{print NR}'` against `git show HEAD:<file>` and the
working tree, not with `wc -l` alone — `wc -l` counts newlines, so a file whose
final newline was added or removed can read identically while its line count
changed. The diff is **7 insertions, 7 deletions**, in four hunks.

## Chain

```
bun run build                      exit 0
bun test                           1156 tests / 1142 pass / 0 fail / 14 skip / 4220 expect()
bun run lint                       0 violations across 126 files
bun run typecheck                  exit 0
bun run skill-lint                 4 SKILL.md + 12 contract files, 0 errors
bun run claim-lint                 125 files, 0 errors, 1 warning
bun run skill-harness              exit 0
scripts/verify-required-checks.sh  PASS, SIX contexts
scripts/verify-workflow-hygiene.sh PASS
```

## The four restatements

### `M2.md:107-109` — the sentence that was vacuous in one direction

Before:

> **Verification:** Each skill runs twice against the same fixture repository and
> the second run makes zero mutations; the Design and plan produced are parsed
> mechanically and every claim maps to at least one case; both lints pass.

After:

> **Verification:** Per scenario and never summed: run 1 makes **at least one**
> mutation and run 2 then makes **none** on the same fixture repository; Design
> and plan parse mechanically, every claim maps to a case; both lints pass.

*"Zero mutations"* with no `> 0` beside it is **satisfied perfectly by a harness
that never ran anything** — the exact vacuity #322's runner was built to close,
left standing in the milestone sentence that seeds it. **Per scenario, never
summed** matters for the same reason: a roster summing to one mutation across
six scenarios would pass a summed test while five scenarios did nothing.

**A drafting error worth recording.** The first version of this rewrite
compressed the tail to *"the Design and plan parse"*, which silently dropped
**"every claim maps to at least one case"** — a verification requirement, not a
turn of phrase. Caught by diffing meaning rather than counting lines. A
line-neutral edit can be line-neutral and still delete a requirement.

### `M2.md:105` — the build-target assignment

Before: `| … | iai-skills | Shared by S2.2 through S2.5 |`
After: `| … | iai-harness | Its own build target, ruled at gate G5 of #293; shared by S2.2–S2.5 |`

`iai-skills` is a `docs` target with **no build file** (`ARCHITECTURE.md:71`,
`:75-76`). Gate ruling **G5** made the harness its own build target; #318 added
the row, verified present at `ARCHITECTURE.md:62` —
`iai-harness | library | packages/harness/package.json | packages/harness/src`.
The name in the milestone was checked against that row, not assumed.

### `M2.md:230-232` — the superseded exit criterion

The bare-token ban (`dev`, `trade`, `health`, `wealth`, `know`) was overturned by
gate ruling **G2 of #41**, whose Decision 4 records it as *unimplementable*
(`know` appears 16 times in `references/`, 13 of them ordinary English) and
*self-contradictory* (the doctrine the skills must state contains four of the
five tokens). It now bans the **routing form** — `domain:<id>` and
`skills/<id>/` for any id in `KNOWN_DOMAIN_IDS`.

**This is the second time the file was opened with this criterion superseded.**
The issue required that this task state which way it goes rather than leave it a
third time. It is fixed, not deferred.

### `M4.md:215` — fixture ownership

Now records that the conformance harness runs over **the one** fixture
repository S2.6 (#293) owns and M4 consumes, per gate ruling **G6** — *"two
fixture repositories is a coexistence problem arriving before anything is
built."*

## The citation audit — line-neutrality preserved every target and destroyed eight truths

**This is the point of the task, and #304's third live instance.** #297's edit
was exactly line-neutral *so that* its citations would keep resolving. They do.
They are also no longer true. A guard that checked only whether a `:NN` still
resolves would have passed it — because making it pass was the design goal.

Every citation into the four edited sites was resolved by hand. All still
**resolve**. Eight are now **false in the present tense**:

| Citation | Says | After this edit |
|---|---|---|
| `docs/design/stories/293.md:194` | `M2.md:105` assigns the harness to `iai-skills` | **false** |
| `docs/design/stories/293.md:400` | assigns the work to `iai-skills`, a `docs` target with no build file | **false** |
| `docs/design/stories/293.md:628` (G5) | `M2.md:105` assigns it to a `docs` target with no build file | **false** |
| `docs/test-plans/293-plan.md:184` | quotes *"Each skill runs twice…"* as what `:107-109` seeds | **false** — verbatim quote of replaced text |
| `docs/design/stories/293.md:227` | `:230-232` **still carries** the bare-token criterion | **false** |
| `docs/design/stories/293.md:591` | the **superseded** bare-token criterion at `:230-232` | **false** |
| `docs/design/stories/293.md:640` | `:230-232` **still carries** wording G2 overturned | **false** |
| `docs/design/stories/293.md:599` | the `M4.md:215` restatement **remains unresolved** | **false** — now resolved |

Still true, and checked rather than assumed:

| Citation | Why it survives |
|---|---|
| `docs/design/stories/41.md:152` | *"sets the verification as an idempotency re-run"* — still is |
| `docs/design/stories/41.md:158` | *"work to be done, not a tool to be used"* — unaffected |
| `docs/test-plans/293-plan.md:21` | *"written against the ruled Design, **not** against `M2.md:107-109`"* — a statement about provenance |
| `docs/test-plans/293-plan.md:177-178` | `M4.md:195` and `:215` own the milestone-to-merged-PR run — still do |
| `packages/harness/src/fixture-repo.ts:13` | cites `M4.md:195, :215` as the consumer — still true, now explicitly |
| `docs/design/stories/293.md:284`, `:629` (G6) | the ruling text remains an accurate record of the ruling |

**No immutable evidence artifact cites any of the four edited sites.** Checked
across all of `docs/evidence/`: the artifacts citing `M2.md` point at `:29`,
`:85`, `:94-96`, `:123-125`, `:132` and `:240-241`, and those citing `M4.md`
point at `:74-76` and `:117-120`. **None was touched.** `docs/design/stories/41.md:238`'s
*"two in immutable evidence artifacts"* refers to the `:29` pair.

### Why the eight were not edited

Every one is a **problem statement or a gate ruling** inside #293's Design and
test plan — gate-approved, pinned documents that record the world **as it was at
ruling time**. A Design that described the defect it commissioned a fix for, and
then quietly stopped describing it once fixed, would destroy the record of why
the fix happened. They are enumerated here instead, which is the only mitigation
this repository currently has.

> **`#304` remains unowned and this task did not close it.** Nothing verifies
> that a `:NN` still says what the citing document claims. Eight citations were
> checked by hand because there is no other way. That is the third consecutive
> task to pay that cost.

## `M2.md:240-241` — deliberately not edited, with an owner

The token-cost exit criterion — *"Measured token cost of Tier 0 plus one Tier-1
verb is recorded in the evidence artifact"* — is **out of this task's scope** and
was left untouched, deliberately.

**Reason:** #324 recorded a figure against it (305.2k / \$36.14) that is an
opencode **session total**, an upper bound covering the construction of the live
rung, the transcription audit and the CI work — not one verb's cost. Whether to
tick the criterion, restate what it bounds, or re-measure is a **ruling**, not a
line edit, and making it silently here would be the third time this file had a
superseded criterion rewritten without one.

**Owner: #337**, which names the cheap fix — one dedicated session running a
single Tier-1 verb end to end, so the session total *is* the verb's cost.
`scripts/live-rung.ts --token-cost` already accepts the figure.

**`M2.md:240-241` must not be ticked on #324's number.**

## What this does NOT establish

- **The eight stale citations are recorded, not fixed.** They will mislead the
  next reader who quotes them without resolving them.
- **#304 is not closed**, and this task is evidence that hand-checking does not
  scale: four edited sites required auditing roughly thirty citations.
- **M2's exit criteria are not met by this edit.** Restating a criterion is not
  satisfying it. `:230-232` now states the rule the linter actually implements;
  whether every Tier-1 skill passes it is S2.2–S2.5's work.
- **`M2.md:240-241` is unresolved** — #337.
