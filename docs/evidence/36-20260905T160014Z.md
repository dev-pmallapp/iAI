---
issue: 36
story: 35
repo: dev-pmallapp/iAI
commit: 761577988c0f138e6193275ff65b8de309a24299
branch: task/36-s2-1-1-context-discovery-gh-operations-gh-error
run: 2026-09-05T16:00:14Z
verdict: PASS
---

# Evidence: #36 — S2.1.1 context-discovery, gh-operations, gh-error-handling

| Field | Value |
|-------|-------|
| Task | #36 (`type:task`), parent #35 |
| Build target | `iai-references` |
| Verdict | **PASS**, with one surviving mutation recorded below |
| Chain | 792 tests / 0 fail / 14 skip / 2590 `expect()`; lint 0 across 91 files; claim-lint 86 files / 0 errors; typecheck, skill-lint clean; purity 9/0; binding sweep 5/0 |

## What shipped

Three of the twelve kernel reference documents, grouped per Decision 4 of
`docs/design/stories/35.md`.

| Document | Bytes | Frontmatter | Tracked |
|---|---|---|---|
| `references/context-discovery.md` | 5400 | none | yes |
| `references/gh-operations.md` | 6435 | none | yes |
| `references/gh-error-handling.md` | 6511 | none | yes |

Absence of frontmatter is a property, not an oversight: these are contracts read
by explicit path, never discovered as skills. It is `CLAIM-35.4`'s second half
(case 10).

## Case results

| Case | Claim | Result | Basis |
|---|---|---|---|
| 1 | `CLAIM-35.1` | **PARTIAL — 3/12, by design** | Only this task's three exist. The 12/12 denominator is a Story-level assertion and cannot pass until #40 |
| 2 | `CLAIM-35.1` | **DEFERRED** | Set equality over `references/` requires all twelve. Nine allow-list entries remain, correctly |
| 10 | `CLAIM-35.4` | **PASS** | None of the three begins with a `---` line; measured, table above |
| 14 | `CLAIM-35.6` | **PASS (partial scope)** | Each document ends with a reconciliation table. Two rows carried with owners; see below |
| 15 | `NEVER-35.7` | **PASS** | 0 restatements over a constant set of **5** — denominator asserted |
| 16 | `NEVER-35.7` | **PASS (manual)** | The rule does not exist until #280; compliance verified mechanically here instead |
| 17 | `NEVER-35.8` | **PASS** | `allowlist-stale` 0; 37 → 34; family header 12 → 9; three count assertions agree |
| 18 | `NEVER-35.8` | **PASS** | Both broken fixtures fixed; `path-refs.test.ts` 63/0 |
| 19 | `NEVER-35.8` | **PASS** | Mutation M1 below |

## `NEVER-35.7` verified mechanically, before the rule exists

The rule that enforces this ships in #280, which is blocked by all five document
tasks. Rather than assert compliance by inspection, the exported constants were
imported and searched for literally across the three documents.

```
constants checked: 5 (must be >= 4)
  references/context-discovery.md: names an owning module+line = false
  references/gh-operations.md:     names an owning module+line = true
  references/gh-error-handling.md: names an owning module+line = true
NEVER-35.7 restatements: 0
```

Constants checked: `SENTINEL_NAMESPACE_PREFIX`, `SUB_ISSUE_FEATURE_HEADER`,
`EXCLUSIVE_LABEL_PREFIXES` (as a literal array), `BACKOFF_BASE_MS`,
`BACKOFF_CAP_MS`. **The denominator is asserted at ≥ 4**, because a
no-restatement check over an empty constant set is a rule that cannot fail.

**`context-discovery.md` names no module, and that is correct, not a miss.** It
is the one of the three with no code counterpart — context discovery is skill
behaviour specified for M2, and `packages/core` exports nothing it could
restate. The check confirms it restates nothing; there is nothing for it to cite.

## Mutation testing

Run against the **committed** tree at `7615779`, per the standing rule that a run
on a dirty working copy is a recollection rather than evidence. `git status
--porcelain` was empty before and after each.

| # | Mutation | Result |
|---|---|---|
| M1 | Reintroduce a retired entry (`context-discovery.md`) | **Killed, over-determined** — `allowlist-stale` 1 violation **and** `path-refs.test.ts` 61/2 |
| M2 | Revert the `:201` fixture to `references/gh-operations.md` | **Killed** — 62/1 |
| M3 | Restore the literal floor `toBeGreaterThan(30)` | **SURVIVED** — 63/0 |

### M3 survived, and the reason matters

The literal floor still passes at 34 entries, because 34 > 30. **The fix cannot
be proven necessary at this task**: the breakage it prevents materialises at
**#38**, where the count reaches 29.

Rather than record an unproven fix, the #38 state was simulated — five further
entries retired (`verification`, `workflow-states`, `branch-and-pr-model`,
`data-classification`, `model-routing`) to reach exactly 29:

| At 29 entries | Result |
|---|---|
| Shipped shape-based assertion | **63 pass / 0 fail** |
| Old literal `toBeGreaterThan(30)` | **62 pass / 1 fail** |

So the replacement is both necessary and sufficient — it simply cannot fire two
tasks early. Recorded as a **survivor with a forward-dated kill**, not as a pass.

> **A mutation that survives because the defect is not yet reachable is not a
> passing mutation.** It is a deferred one, and saying so is the difference
> between evidence and a claim.

## The floor was replaced, not lowered

`packages/core/test/path-refs.test.ts:300` asserted `PATH_ALLOW_LIST.length > 30`
as an anti-vacuity denominator. That is a literal floor on a list whose own
header states it is meant to **shrink** as planned paths come into existence
(#277).

Lowering the number would have re-armed the identical trap for the next Story
that retires a family. The assertion now requires **every declared `AllowReason`
to still be represented** — which survives any single family draining to zero
(`planned` outlives `references/` via the `packages/`, `skills/` and
`docs/parity/` entries) and which an emptied or gutted list cannot satisfy.

## Findings raised, not fixed

| # | Finding | Disposition |
|---|---|---|
| 1 | `docs/design/verification-pass.md` row 35 cites `CONTRIBUTING.md:63` for `{target_dir}`; that line is `{target}`, and the real one is `:64` | **Carried, not corrected.** The register's verdict rows are immutable by Decision 9 of `docs/design/stories/35.md`. Recorded in `references/context-discovery.md` |
| 2 | This is the **second** stale `:NN` citation found during S2.1 | The first was the Design citing `01-skill-hierarchy.md:471` for a rule at `:472`, corrected in `9e34463`. **Finding 26 is an active defect class, not a hypothetical.** Neither is detectable: `path-dangling` asks whether the file exists, and it does |
| 3 | `01-skill-hierarchy.md:54` and `:60` both claim comment upsert | **Resolved** by Decision 7 — argv construction here, sentinel discovery and upsert in `references/evidence-artifacts.md` (#39). `gh-operations.md` states the boundary explicitly rather than silently omitting it |

## Known limits of this verdict

- **`skill-lint` scanned 0 files**, as it has since S1.3. It is required in CI and
  reports success over an empty corpus. `NEVER-35.7` compliance above is
  therefore *manual and mechanical*, not enforced by the shipped toolchain. The
  three body rules land in #280.
- **These three documents are a rework candidate.** #280 is blocked by all five
  document tasks so its denominators are real, which means the twelve are written
  before the rules exist to police them. If the shipped rule is stricter than the
  check above, these three come back.
- `expect()` moved **2598 → 2590**. That is the three parameterised allow-list
  entries retiring plus the denominator assertion changing shape — not lost
  coverage.
- **Every number in this artifact is measured at `7615779`, the implementation
  commit named in the frontmatter — not at the commit that adds this file.**
  Committing this artifact takes `expect()` from 2590 to **2592**, because a case
  in `packages/core/test/` reads `docs/evidence/` at run time and counts what it
  finds. The same self-reference is recorded at `docs/test-plans/26-plan.md:89`.
  A verification artifact changes the tree it verifies; stating which tree the
  numbers belong to is the only way the arithmetic reconciles.
