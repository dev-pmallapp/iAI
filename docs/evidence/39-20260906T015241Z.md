---
issue: 39
story: 35
repo: dev-pmallapp/iAI
commit: 49a71f7e95d080051c5ae32397e736272f090cdb
branch: task/39-s2-1-4-domain-binding-evidence-artifacts
run: 2026-09-06T01:52:41Z
verdict: PASS
---

# Evidence: #39 — S2.1.4 domain-binding, evidence-artifacts

| Field | Value |
|-------|-------|
| Task | #39 (`type:task`), parent #35 |
| Build target | `iai-references` |
| Verdict | **PASS** |
| Chain | 792 tests / 0 fail / 14 skip / 2575 `expect()`; lint 0 across 91 files; claim-lint 89 files / 0 errors; typecheck, skill-lint clean; purity 9/0; binding sweep 5/0 |

## What shipped

| Document | Bytes | Frontmatter |
|---|---|---|
| `references/domain-binding.md` | 8032 | none |
| `references/evidence-artifacts.md` | 7776 | none |

Allow-list **29 → 27**; family header **4 → 2**. Ten of twelve documents on disk;
`design-format` and `sizing-criteria` remain. Stacked on `task/38`; PR #284
targets the story branch. Merge order **#281 → #282 → #283 → #284**.

## Finding 28 discharged

`docs/design/01-skill-hierarchy.md:60` makes `evidence-artifacts.md` the owner of
the sentinel namespace, the blob-SHA pattern and both budget numbers.
`NEVER-35.7` forbids restating any constant `packages/core` exports.

**Its subject is a set of exported constants.** The document may not contain the
things it defines.

Discharged as case 16 specifies — it describes every one and contains none,
naming the owning module and line instead. **9 constants checked, 0
restatements**: the sentinel prefix, the nine-name array, the blob pattern, the
working budget, the hard limit, the derived envelope allowance, the known-domain
array, the exclusive label prefixes, and the sub-issue feature header.

This is the largest denominator used for `NEVER-35.7` in the Story, and
deliberately so: the weakest form of this check would have been to run it over
constants the document was never at risk of restating.

## Mutation testing

Against the **committed** tree at `49a71f7`. `git status --porcelain` empty
before and after each.

| # | Mutation | Shipped toolchain | Ad-hoc check | Verdict |
|---|---|---|---|---|
| M1 | Reintroduce a retired allow-list entry | `allowlist-stale` 1 **and** `path-refs.test.ts` 61/2 | – | **KILLED**, over-determined |
| M2 | Write the working-budget **value** into the document whose subject it is | **entirely green** | **caught** | **SURVIVED** (toolchain) |
| M3 | Delete an entire reconciliation table | **entirely green** | not applicable | **SURVIVED — correctly** |

### M2 — the sharpest form of the `NEVER-35.7` gap

Writing `60000` into `references/evidence-artifacts.md` — the single most
on-the-nose violation available, in the one document whose job is to describe
that number without containing it — passes `bun test`, `lint`, `skill-lint` and
`claim-lint`.

Only the ad-hoc importer catches it, and that script is not in the repository.

This is the fourth measured instance of the same gap:

| Claim | Proven unenforced by |
|---|---|
| `NEVER-35.7` | #37 M2, and **#39 M2** in its sharpest form |
| `CLAIM-35.4` case 10 | #37 M3 |
| `CLAIM-35.3` | #38 M2 |

All land on #280. **Two more documents (#40) are authored before it.**

### M3 — a survivor that is correct, and the distinction matters

Deleting a whole reconciliation table passes everything. **That is not a gap.**

`CLAIM-35.6` is the Story's only `model-judged` claim, and deliberately so — a
reconciliation is a judgement about meaning, and calling it `tool-checked` would
be the vacuity this Story exists to prevent. A tool-checked mutation surviving
against a model-judged claim is the taxonomy working, not failing.

> **Not every surviving mutation is a defect.** Three kinds have now appeared in
> this Story: a survivor because the defect is not yet reachable (#36 M3, killed
> later at #38), a survivor because nothing enforces the claim (#37, #38, #39
> M2), and a survivor because the claim is not tool-checkable **by design**
> (#39 M3). Recording which kind is the whole value of the exercise.

## A false claim caught before it shipped

The first draft of `references/domain-binding.md` asserted that
`CONTRIBUTING.md:303` **still** instructs a pack to register itself by editing
`packages/core/src/binding/registry.ts`.

It does not. Decision 2 of `docs/design/stories/31.md` **corrected that checklist
during S1.5**; `CONTRIBUTING.md:303-312` now names the composition root and
explicitly forbids editing `registry.ts`.

The source of the error: the finding list describes the **original** conflict,
and it was read as a description of current state.

> **A stale finding read as present tense is the same defect class as a stale
> `:NN`** — trusting a note instead of resolving it — but it produces a
> confidently wrong statement about the repository rather than a wrong pointer.
> Recorded as finding 33. Every row in a findings list is a historical record of
> what was found, **not an assertion about HEAD.**

Caught by resolving all 27 citations in the two documents against their targets
before commit. Every one of the 27 was verified; this was the only semantic
error among them.

## Case results

| Case | Claim | Result | Basis |
|---|---|---|---|
| 1 | `CLAIM-35.1` | **PARTIAL — 10/12** | Cumulative. 12/12 reachable at #40 |
| 2 | `CLAIM-35.1` | **DEFERRED** | Two entries remain allow-listed, correctly |
| 10 | `CLAIM-35.4` | **PASS by measurement, UNENFORCED** | Both documents lack frontmatter |
| 14 | `CLAIM-35.6` | **PASS** | Reconciliation table per document; six divergences carried with named owners in `domain-binding.md`, five in `evidence-artifacts.md`. **Zero unowned** |
| 15 | `NEVER-35.7` | **PASS over a denominator of 9** | The Story's largest |
| 16 | `NEVER-35.7` | **PASS by ad-hoc check, UNENFORCED** | M2 |
| 17 | `NEVER-35.8` | **PASS** | `allowlist-stale` 0; 29 → 27; family 4 → 2 |
| 18 | `NEVER-35.8` | **PASS** | `path-refs.test.ts` 63/0 at 27 entries |
| 19 | `NEVER-35.8` | **PASS** | Mutation M1 |

## Findings

| # | Finding | Disposition |
|---|---|---|
| 1 | A stale finding read as present tense produced a false claim about `CONTRIBUTING.md:303` | **Caught and corrected.** Recorded as finding 33; applies to every row of the session findings list |
| 2 | `NEVER-35.7` unenforced, in its sharpest possible form (M2) | **Carried to #280** |
| 3 | **The hard limit has no source in this repository** | **Carried.** Marked at `packages/core/src/evidence/render.ts:42-56` as an explicitly unverified inherited constant; recorded in the document |
| 4 | Six divergences between the shipped binding interface and the design documents | **All carried with named owners** — M3, M4, M5, M6, M2, and each pack's Story. Zero unowned, per Decision 6 |

## Known limits of this verdict

- **`skill-lint` scanned 0 files.** Unchanged since S1.3.
- **Three claims verified by measurement and an uncommitted script.** Nothing
  prevents regression in #40.
- **These ten documents are rework candidates** if #280's shipped rules prove
  stricter than the checks used here.
- Numbers are measured at `49a71f7`. Committing this artifact takes `expect()`
  from 2575 to **2577**, because a case reads `docs/evidence/` at run time.
