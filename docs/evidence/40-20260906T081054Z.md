---
issue: 40
story: 35
repo: dev-pmallapp/iAI
commit: a8d591f5754d43597bb22b6d93e2ab8f86959e8a
branch: task/40-s2-1-5-design-format-sizing-criteria
run: 2026-09-06T08:10:54Z
verdict: PASS
---

# Evidence: #40 — S2.1.5 design-format, sizing-criteria

| Field | Value |
|-------|-------|
| Task | #40 (`type:task`), parent #35 |
| Build target | `iai-references` |
| Verdict | **PASS** — deliverables correct. **One unowned gap found; see the finding below.** |
| Chain | 792 tests / 0 fail / 14 skip / 2571 `expect()`; lint 0 across 91 files; claim-lint 90 files / 0 errors; typecheck, skill-lint clean |

## What shipped

| Document | Bytes | Frontmatter |
|---|---|---|
| `references/design-format.md` | — | none |
| `references/sizing-criteria.md` | — | none |

**All twelve kernel reference documents now exist.** Allow-list **27 → 25**; the
`references/*.md` family section **deleted**; header `Seven families, 27 entries.`
→ **`Six families, 25 entries.`**

## Four cases verifiable for the first time

Cases 1, 2, 9 and 10 require the full population and could not pass before this
task. All four pass.

| Case | Result | Method |
|---|---|---|
| 1 | **PASS** | The twelve names are **parsed from `docs/milestones/M2.md` at run time**, never restated. The parse is asserted at exactly 12 **before** any existence check, so a failed parse cannot pass vacuously |
| 2 | **PASS** | Set equality **both directions**: 12 on disk, 12 expected, 0 extra, 0 missing. `design-format.md` present and `isa-format.md` absent — the gate rename asserted, not assumed |
| 9 | **PASS** | The roster-discovery predicate returns **0 over a denominator of 12 real files**. The denominator is the case: an empty directory also returns zero |
| 10 | **PASS** | 0 of 12 carry frontmatter |

`CLAIM-35.3` across all twelve: **7 model-ID hits in `model-routing.md`, 0 in the
other eleven.** `NEVER-35.7` over the final two: **0 restatements across 9
constants.**

## Mutation testing

Against the **committed** tree at `a8d591f`.

| # | Mutation | Shipped toolchain | Verdict |
|---|---|---|---|
| M1 | Reintroduce a retired entry, re-creating the family section | `allowlist-stale` 1 **and** `path-refs.test.ts` 61/2 | **KILLED** |
| M2 | Header claims `Seven families` while six sections remain | **`the header's family count matches the number of family sections` FAILS**, 62/1 | **KILLED** |
| M3 | Add a **thirteenth** file to `references/` | **entirely green** | **SURVIVED** |

**M2 is the assertion this task exercised for the first time.** Four previous
tasks changed the family's *entry count*; only this one changed the *number of
families*. It fires correctly.

---

## FINDING: `CLAIM-35.1` is tool-checked on paper, unenforced in fact, and **unowned**

M3 is not the same kind of survivor as those on #37–#39. Those gaps are
**unenforced but owned** — #280 ships the rules. This one has no owner at all.

Verified three ways:

| Question | Answer |
|---|---|
| How does the test plan classify `CLAIM-35.1`? | **`tool-checked`**, tool `bun test` (cases 1 and 2) |
| Does a committed test reference the twelve, or `CLAIM-35`? | **No.** `grep -rn "CLAIM-35" packages/core/test/ test/` returns nothing |
| Does any Build Target ship one? | **No.** The Design's Build Targets table has no row for a `CLAIM-35.1` test |
| Does #280 cover it? | **No.** Its Claims table lists `CLAIM-35.2`, `.3`, `.4`, `.5` and `NEVER-35.7` — not `.1` |

So the Story's **first claim** — "all twelve files exist under the exact names" —
is declared tool-checked, is verified in this artifact only by an **ad-hoc script
that is not in the repository**, and no task will ever ship the test.

A thirteenth file, a misspelling, or a reverted `isa-format.md` would all pass CI.

> **This is the fifth consecutive Story where enforcement is under-counted — and
> the first where the DESIGN'S OWN Build Targets table is the thing that missed
> it**, rather than the milestone's indicative task table. The Design documents
> that exact pattern (S1.3 → #253, S1.4 → #261, S1.5 → #272, S2.1 → #280) and
> then reproduced it one level up.

**Not acted on.** Adding a claim to #280's scope, or cutting a seventh task, is a
scope decision and belongs to the human decider. The recommendation is recorded
on #40 and in the handoff.

The reproducible check used here is the same shape as case 1's specification —
parse the names from the milestone, assert the parse at 12, then compare sets:

```ts
const row = m2.split("\n").find(l => l.includes("The twelve `references/*.md` contract documents"));
const names = (row.split("restate:")[1] ?? "").replace(/\|/g,"")
  .split(/,| and /).map(s => s.trim().replace(/\.$/,"")).filter(Boolean);
if (names.length !== 12) throw new Error(`parse yielded ${names.length}, refusing to assert`);
const onDisk = readdirSync("references").filter(f => f.endsWith(".md")).sort();
const expected = names.map(n => `${n}.md`).sort();
// case 1: names.every(n => onDisk.includes(n + ".md"))
// case 2: JSON.stringify(onDisk) === JSON.stringify(expected)
```

---

## Case results

| Case | Claim | Result |
|---|---|---|
| 1 | `CLAIM-35.1` | **PASS by ad-hoc check — UNENFORCED and UNOWNED** |
| 2 | `CLAIM-35.1` | **PASS by ad-hoc check — UNENFORCED and UNOWNED** |
| 9 | `CLAIM-35.4` | **PASS over a denominator of 12** — unenforced, owned by #280 |
| 10 | `CLAIM-35.4` | **PASS** — unenforced, owned by #280 |
| 14 | `CLAIM-35.6` | **PASS** — reconciliation table per document, zero *unowned* content conflicts |
| 15 | `NEVER-35.7` | **PASS over a denominator of 9** |
| 17 | `NEVER-35.8` | **PASS** — 27 → 25, family retired to 0, header `Six families` |
| 18 | `NEVER-35.8` | **PASS** — 63/0; M2 confirms the family-count assertion fires |
| 19 | `NEVER-35.8` | **PASS** — M1 |

## Findings

| # | Finding | Disposition |
|---|---|---|
| 1 | **`CLAIM-35.1` is unenforced and unowned** | **Needs a human decision** — add to #280 or cut a seventh task |
| 2 | The seventeen Design body sections are *ordered, not fixed*, contradicting `docs/milestones/M2.md:85` | **Carried** in `design-format.md`. Owner `CLAIM-41.3`, S2.2 |
| 3 | `minSize`/`maxSize` are strings, so `size` is model-judged not tool-checked | **Carried** in `sizing-criteria.md`. Decision 6 of `docs/design/stories/31.md`; owner M2 |
| 4 | `gh pr create` failed with a connection error mid-task | Checked whether the PR had been created (it had not) **before** retrying. Blind retry would have risked a duplicate |

## Known limits of this verdict

- **`skill-lint` scanned 0 files.** Unchanged since S1.3.
- **All twelve documents were authored with four claims unenforced.** Three are
  owned by #280; `CLAIM-35.1` is owned by nobody.
- Numbers measured at `a8d591f`. Committing this artifact takes `expect()` from
  2571 to **2573**.
