---
issue: 26
story: 26
repo: dev-pmallapp/iAI
commit: 2dfbbab137dfab6bddff13f1a960019057844ecd
branch: story/26-s1-4-evidence-and-sentinel-engine
run: 2026-09-04T04:17:12Z
verdict: PASS
---

# Evidence: #26 — S1.4 Evidence and sentinel engine, story verification

| Field | Value |
|-------|-------|
| Story | #26 |
| Claims | CLAIM-26.1 – CLAIM-26.6, NEVER-26.7 – NEVER-26.10 |
| Cases | 26 — 24 P0, 2 P1 |
| Tasks | #27, #28, #29, #30, #261 — all resolved, merged and closed |
| Verdict | **PASS** — 24/24 P0, 2/2 P1 |

Run on the integrated story branch at `2dfbbab`, after all five task PRs merged.
Every case was executed under its own escaped `-t` filter, and the denominator
is stated for each.

## Verification chain

```
build       0
test        692 pass / 0 fail / 8 skip   (700 across 30 files)
lint        0 violations across 75 files
typecheck   0
skill-lint  0 files, 0 errors
claim-lint  75 files, 0 errors
```

```
no-io-in-pure-modules  0 violations  (scope: core/src/classify, core/src/guards, core/src/gh, core/src/evidence)
```

If `core/src/evidence` is ever absent from that line, #261 has been reverted and
`NEVER-26.7` is enforced by nothing again.

## Claim results

| Claim | Cases | Result |
|-------|-------|--------|
| CLAIM-26.1 | 1, 2, 3 | PASS — nine names accepted at column zero; five producer rules rejected with five pairwise-distinct messages |
| CLAIM-26.2 | 4, 5, 6 | PASS — newest by `createdAt` wins, nothing merged or averaged, ties broken by highest id over both input orderings |
| CLAIM-26.3 | 7 | PASS — absence is a blocking `Decision`; no default, no `undefined`, no empty body |
| CLAIM-26.4 | 8, 9, 10, 11, 12 | PASS — 59,999 and 60,000 inline, 60,001 summarises with a pinned permalink, no body over 65,536, envelope within 5,536 |
| CLAIM-26.5 | 13, 14 | PASS — 7-hex through 40-hex accepted, 6 and 41 and uppercase rejected, every branch-named form rejected by name |
| CLAIM-26.6 | 15, 16, 17, 18 | PASS — all artifacts on disk reproduced, `{n}` fails loudly, run 2 edits run 1, three non-conforming `{ts}` forms rejected |
| NEVER-26.7 | 19, 20, 21 | PASS — directory in scope over a denominator of 9, barrel exercised under the trapped runtime, rule provably fires |
| NEVER-26.8 | 22 | PASS — 928 hostile invocations across 25 exported functions, 0 throws |
| NEVER-26.9 | 23, 24 | PASS — every edit target carries the requested sentinel; exactly one instruction per run |
| NEVER-26.10 | 25, 26 | PASS — every over-budget rendering carries a pinned permalink; line 1 is always the sentinel |

## Case results, each with its denominator

| # | Pri | Tests | `expect()` | File | Filtered out |
|---|-----|-------|-----------|------|--------------|
| 1 | P0 | 7 | 43 | evidence-sentinel | 15 |
| 2 | P0 | 11 | 47 | evidence-sentinel | 11 |
| 3 | P0 | 4 | 29 | evidence-sentinel | 18 |
| 4 | P0 | 6 | 16 | evidence-consumer | 19 |
| 5 | P0 | 5 | 15 | evidence-consumer | 20 |
| 6 | P0 | 5 | 11 | evidence-consumer | 20 |
| 7 | P0 | 9 | 82 | evidence-consumer | 16 |
| 8 | P0 | 2 | 6 | evidence-render | 31 |
| 9 | P0 | 2 | 7 | evidence-render | 31 |
| 10 | P0 | 4 | 10 | evidence-render | 29 |
| 11 | P0 | 4 | 12 | evidence-render | 29 |
| 12 | P1 | 4 | 8 | evidence-render | 29 |
| 13 | P0 | 5 | 19 | evidence-render | 28 |
| 14 | P0 | 5 | 18 | evidence-render | 28 |
| 15 | P0 | 6 | 61 | evidence-upsert | 21 |
| 16 | P0 | 6 | 38 | evidence-upsert | 21 |
| 17 | P0 | 4 | 21 | evidence-upsert | 23 |
| 18 | P1 | 5 | 21 | evidence-upsert | 22 |
| 19 | P0 | 5 | 18 | test/lint | 43 |
| 20 | P0 | 1 | 12 | purity (child) | 7 |
| 21 | P0 | 2 | 13 | test/lint | 46 |
| 22 | P0 | 7 | 265 | evidence-purity | 0 |
| 23 | P0 | 3 | 9 | evidence-upsert | 24 |
| 24 | P0 | 3 | 31 | evidence-upsert | 24 |
| 25 | P0 | 2 | 13 | evidence-render | 31 |
| 26 | P0 | 5 | 88 | evidence-render | 28 |

**122 tests, 913 `expect()` calls, 0 fail.**

## The cases partition the suite exactly

The strongest denominator statement available, and it holds: the per-case counts
sum to each file's total with nothing left over and nothing counted twice.

```
evidence-sentinel  22 = 7 + 11 + 4                      (cases 1-3)
evidence-consumer  25 = 6 + 5 + 5 + 9                   (cases 4-7)
evidence-render    33 = 2+2+4+4+4+5+5 + 2+5             (cases 8-14, 25, 26)
evidence-upsert    27 = 6+6+4+5 + 3+3                   (cases 15-18, 23, 24)
evidence-purity     7 = 7                               (case 22)
                  ---
                  114 tests, 870 expect()
test/lint.test.ts   5 + 2   (cases 19, 21; 48-test file)
purity child        1       (case 20; 8-test child run)
```

Every test in the evidence suite belongs to exactly one case, and every case
owns at least one test. A case cannot have passed while running nothing, and no
test is orphaned from the plan.

## Standing checks

| Check | Result |
|-------|--------|
| Evidence immutability, `--diff-filter=MD main...HEAD -- docs/evidence` | **0 files.** Unfiltered returns the 5 artifacts this Story legitimately added — Decision 12, and the reason the filter is mandatory |
| Required contexts, `scripts/verify-required-checks.sh` | PASS — `main` requires exactly the six named contexts |
| Workflow hygiene, `scripts/verify-workflow-hygiene.sh` | PASS — six jobs, no job-level `if:`, no matrix, no path or branch filters |
| Design gate | `gate:approved` on #26, decision recorded in the `## iai-gate` sentinel |
| Task closure | #27, #28, #29, #30 and #261 all `status:resolved` and closed explicitly; closing keywords do not fire on story-branch merges |

## Verified against live GitHub data: the engine reads its own Story

The strongest end-to-end check available for this layer is that S1.4's output is
valid input to S1.4. The whole comment stream of this Story — six issues, 13
real comments fetched from the API — was run through the shipped modules.

```
namespace       9 names, prefix-free = true

#26    3 comments, 3 sentinel [design, test-plan, gate], 0 violations
#27    2 comments, 1 sentinel [evidence],                0 violations
#28    2 comments, 1 sentinel [evidence],                0 violations
#29    2 comments, 1 sentinel [evidence],                0 violations
#30    2 comments, 1 sentinel [evidence],                0 violations
#261   2 comments, 1 sentinel [evidence],                0 violations

13 real comments, 8 sentinel-bearing, 0 lint violations, 0 malformed,
7 carrying permalinks, 0 unpinned
```

**Every sentinel comment this Story produced passes the linter this Story
shipped, and every permalink in them is SHA-pinned.** `CLAIM-26.1` and
`CLAIM-26.5` are discharged against real artifacts rather than fixtures.

The consumer over the live #26 stream:

```
design     -> id=5520312691 matchCount=1 found=1   require: allow
test-plan  -> id=5520470796 matchCount=1 found=1   require: allow
gate       -> id=5520503257 matchCount=1 found=1   require: allow
evidence   -> absent                               require: block
verdict    -> absent                               require: block
checkpoint -> absent                               require: block
risk       -> absent                               require: block
effort     -> absent                               require: block
learnings  -> absent                               require: block
```

`CLAIM-26.3` in production: six of the nine sentinels are genuinely absent from
#26 and each returns a blocking `Decision` rather than a default. The `verdict`
row is the one this run is about to fill.

**Newest-wins agrees with the API's own ordering, 8/8**, across every
(issue, sentinel) pair present in the corpus — the selector was compared against
an independent reduction over `created_at` rather than against itself.

**Upsert against the live #261 stream:** the plan is `edit`, and three
consecutive re-runs leave `2 comments before, 2 after, evidence sentinels = 1`.
`docs/milestones/M1.md:191`'s "a double producer run leaves exactly one comment
per sentinel" is discharged against a real comment list.

**`NEVER-26.9` over the real corpus:** 54 (issue, sentinel) pairs planned, 8
producing an edit instruction, **0 whose target lacks the requested sentinel**,
0 producing more than one instruction.

**Templating: 27/27 artifact filenames on disk reproduced exactly**, parsed into
parts and rendered back through `docs/evidence/{issue}-{ts}.md`.

## Findings recorded, none silently fixed

**1. `docs/milestones/M1.md:166` still says the linter rejects "those four
violations".** The Design's Decision 3 and `docs/design/03-workflow.md:388-392`
define **five** producer rules, and cases 2 and 3 enforce five with five
distinct messages. The milestone seeded the claim and is upstream of the Design;
the Design is authoritative per `PLAN.md:200-203`. Recorded, not edited.

**2. `docs/milestones/M1.md:181-187` lists four indicative tasks for S1.4 and
carries no row for purity enforcement** — the identical shape of the gap that
forced #253 into existence net-new against S1.3. S1.4 shipped **five** tasks
anyway, because `docs/design/stories/26.md` carried the scope widening and the
harness import as Build Targets from the start. **This is now a confirmed
pattern rather than a one-off**: the milestone's task table under-counts
enforcement work, and the Design is the only document that catches it. The next
Story should assume the same gap until the milestone template changes.

**3. `docs/test-plans/26-plan.md:89` and note 5 cite "22 filenames currently in
`docs/evidence/`".** There are now 27. Case 15 reads the directory at run time,
so the case does not drift — only the plan's prose does. The plan is
pre-registered; recorded rather than corrected.

**4. The plan's own case arithmetic is correct.** "26 cases — 24 P0 / 2 P1 / 0
P2" agrees with the case tables and with this run: only cases 12 and 18 are P1.
Stated explicitly because the equivalent count in `docs/test-plans/21-plan.md:60`
was wrong and #21's verdict had to record it.

**5. No acceptance checkbox in `docs/milestones/M1.md` has ever been ticked** —
0 of 36, including for the three Stories already merged to `main`. That is the
existing convention, so this Story does not break it, but it means the milestone
file cannot be read as a progress record. Recorded so it is a decision rather
than an oversight.

## Three defects the Story found in itself

Not regressions — defects in the code under test, found by the plan's own cases
during task verification and fixed before merge. All three came from case 22's
reflective sweep in #261:

1. **`sentinelNamesArePrefixFree` hung** on a 200,000-character string handed
   where an array was expected — 4x10^10 comparisons, not a throw. A guard that
   never returns stalls every turn.
2. **`sentinelFor` threw while building a failure message**, so a hostile
   `toString` ran arbitrary code while constructing the very typed-failure value
   that was supposed to replace a throw.
3. **`applyUpsert` threw on a non-array comment list**, and the guard that fixes
   it is reachable only by a hand-written test — the reflective sweep is blocked
   by the action guard in front of it.

Across the Story, **mutation testing found something a green suite did not in
all five tasks**. The survivor classes are now documented: a missing assertion
(#27, #29), a fixture that cannot distinguish two keys (#28), an equivalent
mutant (#29), an unbalanced-brace blind spot (#30), and a guard shadowed by
another guard (#261).

## Not verified here

- **Anything an adapter executes.** This layer returns instructions and
  rendered strings. Nothing in the Story posts a comment, writes a file or
  resolves a SHA. The live checks above read the API and compute; they write
  nothing.
- **The gate-collision case.** Two different gate kinds on one issue collide
  under the `(issue, sentinel)` key. Decision 10 assigns it to M3's gate
  lifecycle (`CLAIM-75.1`); it cannot be observed from here.
- **`DomainBinding` registry validation.** S1.5 (#31) enforces these constants
  against a binding; this Story only exports them.
- **Obfuscated I/O.** `globalThis["pro" + "cess"]` defeats the lint rule in all
  four pure directories. The purity harness catches it, so exposure is equal
  across the four and already mitigated; no new rule is proposed here.
- **A time bound on any predicate.** #261's mutation 7 showed a hang is not a
  throw and no assertion covers non-termination. Nothing in this layer sits on a
  guard hot path, so `CLAIM-15.5`'s 50 ms budget does not bind; generalising it
  belongs to whoever owns `CLAIM-15.5`.
- **Retrofitting the 27 artifacts on disk.** They are immutable. Case 15 and the
  live templating check read their filenames and change nothing.

<sub>Verified by iAI, hand-executed, on the integrated story branch. Run
independently of the task-level claims, per `docs/design/02-roles.md:610-612`.</sub>
