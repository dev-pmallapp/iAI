---
issue: 297
repo: dev-pmallapp/iAI
branch: task/297-reconcile-design-format
commit: e67758366bebd850c6d66517749d39c7b10fa9bf
run: 2026-09-07T03:58:58Z
verdict: PASS
---

# Evidence: #297 — reconciling `design-format.md`, and what line-neutrality does not buy

| Field | Value |
|-------|-------|
| Verdict | **PASS** |
| Task | S2.2.9. Implements **Build Target 10**; discharges the `carried` row `CLAIM-41.3` owned |
| Chain | 903 tests / 889 pass / 0 fail / 14 skip · lint 0 across 95 files · typecheck 0 · skill-lint 4 SKILL.md, 0 errors · claim-lint 110 files, 0 errors, 1 warning |
| `expect()` | **3156 before this artifact lands; 3158 after** — #299, eighth confirmation |
| Line-neutrality | `design-format.md` **14 for 14**, `M2.md` **1 for 1**; both files unchanged in length |
| Cases | 2, 16, 17 — the regression surface — all PASS |

## What changed

| Line | Was | Now |
|---|---|---|
| `:31` | *"Four keys: `phase`, `progress`, `task`, `slug`."* | no YAML frontmatter; the H1-plus-bolded-metadata convention, ruled **G1b** |
| `:38-52` | *"The body reaches **seventeen** sections"*, disagreement **carried** | the **ten-section ordered spine**, measured 9 of 9, ruled **G1a** |
| `:143` | `confirmed` | **corrected** — G1b, with the 1-of-9 outlier stated |
| `:144` | `confirmed` | **corrected** — G1a, the measured spine is ten |
| `:145` | **carried**, owner `CLAIM-41.3` | **discharged**, citing Decision 3 and G1a |
| `M2.md:85` | *"all seventeen body sections"* | **annotated**, not falsified |

## The spine is cited, not copied

`design-format.md` now points at `packages/core/src/guards/design-spine.ts` for
the ten names rather than listing them — the same posture
`references/evidence-artifacts.md` takes toward the sentinel prefix, which it
cites at `sentinel.ts:53` rather than writing.

> A second list is one that drifts from the rule that checks it.

That is **#305 in one sentence**, and it is why #305 was deferred rather than
guessed: the question it asks — *can the markdown form be banned without making
the owning document unwritable* — is answered here in the only way that does not
create the bind. **The owning document does not restate it either.**

## Line-neutrality was required, and it was not enough

The edit was written to preserve line numbers. `:31`, `:52` and `:145` still
exist and still hold the statements they held roles for. Both files changed
**1:1** and **14:14** and are the same length as before.

**This was not incidental.** Those lines are cited from the Design, the test
plan, a core guard comment, and **two immutable evidence artifacts**;
`M2.md` is indexed positionally by roughly thirty citations, two of them in
`docs/evidence/`.

**And it still broke every citation that mattered.**

| Citation | Asserts | After #297 |
|---|---|---|
| `docs/design/stories/41.md:35` | *"not the four at `design-format.md:31`"* | `:31` names no keys at all |
| `docs/test-plans/41-plan.md:140` (case 17) | *"not the four `:31` specifies"* | same |
| **`docs/evidence/294-*.md:57`** | *"`:31` specifies four"* | same — **and the artifact is immutable** |

Every one of them still **resolves**. None of them is still **true**.

> **This is the sharpest evidence yet for #304.** A stale-`:NN` guard that only
> checked the line still existed would have passed this edit **unchanged** —
> because making it pass was the entire design goal. The guard has to check what
> the line *says*, and #304 must be scoped accordingly.

Recorded as **plan amendment 6**. The case rows are **not rewritten**, per the
posture of amendments 1-5: they are the record of what was planned, not
assertions about HEAD (finding 33).

## A citation broken inside the change that caused it

`packages/core/src/guards/design-spine.ts:26` said
*"`references/design-format.md:38-52` **carries the disagreement** and `:145`
names `CLAIM-41.3` as its owner"*. This commit made both clauses false, and
corrects them in the same commit.

**Second time in three tasks** — #296 corrected two `path-refs.ts` citations
that #295 had broken one day earlier. The difference is that this one was
predicted: the citation was found *before* the edit, because #297's own issue
warned that this file is positionally indexed. **The warning is what made the
difference, and nothing automated produced it.**

## Cases 2, 16 and 17

All three read this document's rule as their specification, which makes them the
regression surface. All pass unchanged. Case 17's assertion — that both
frontmatter conventions in the corpus are accounted for, 8 of 9 plus the
recorded outlier — is now described by `:31` rather than contradicted by it.

The `docs/design/stories/9.md` **warning remains a warning** (110 files, 0
errors, 1 warning). Gate ruling G1b is "recorded rather than failed", and #297
does not harden it.

## What this closes, and what it does not

**Closes:** the last `carried` row in `design-format.md`'s reconciliation table,
and the contradiction `story-design` would otherwise have shipped pointing at —
a contract stating seventeen sections while the linter enforcing its output
requires ten.

**Does not close:**
- **#305** — `DESIGN_SPINE` is still unprotected by `duplicate-contract`. This
  task removed the most likely place a copy would have appeared, which lowers
  the risk without closing the gap.
- **#304** — and this task is the best argument yet for its scope.
- **#307** — `CLAIM-41.5` remains unruled.
