---
issue: 38
story: 35
repo: dev-pmallapp/iAI
commit: 6d57de721f9f4bf50148cddfcb794aa121a00f98
branch: task/38-s2-1-3-data-classification-model-routing
run: 2026-09-05T16:52:29Z
verdict: PASS
---

# Evidence: #38 — S2.1.3 data-classification, model-routing

| Field | Value |
|-------|-------|
| Task | #38 (`type:task`), parent #35 |
| Build target | `iai-references` |
| Verdict | **PASS** — deliverables correct; **three claimed properties remain unenforced by CI**, now measured rather than asserted |
| Chain | 792 tests / 0 fail / 14 skip / 2579 `expect()`; lint 0 across 91 files; claim-lint 88 files / 0 errors; typecheck, skill-lint clean; purity 9/0; binding sweep 5/0 |

## What shipped

| Document | Bytes | Frontmatter |
|---|---|---|
| `references/data-classification.md` | 5995 | none |
| `references/model-routing.md` | 6184 | none |

Allow-list **31 → 29**; family header **6 → 4**. Eight of twelve documents now on
disk. Stacked on `task/37`; PR #283 targets the story branch. Merge order
**#281 → #282 → #283**.

## The 29-entry breach, and #36's forward-dated kill landing

This is the task the arithmetic predicted would cross the old literal floor.

| At 29 entries | Result |
|---|---|
| Shipped shape-based assertion | **63 pass / 0 fail** |
| Old literal `toBeGreaterThan(30)` (M3) | **62 pass / 1 fail** |

`docs/evidence/36-20260905T160014Z.md` recorded M3 as a **survivor with a
forward-dated kill**, having proved it only against a *simulated* 29-entry state.
Two tasks later the real state arrived and the prediction was exact.

> The value of recording a survivor honestly is that it becomes a falsifiable
> prediction. Had #36 claimed a pass, nothing here would have tested it.

Had the floor been **lowered** rather than **replaced**, the identical trap would
now be armed for #39, #40, and the next Story that retires a family.

## `CLAIM-35.3` measured over a real denominator

`references/model-routing.md` is the only document in the authored surface
permitted a literal model ID.

| File | model-ID hits |
|---|---|
| `references/model-routing.md` | **7** |
| the other **seven** references | **0** each |

Denominator: 8 files. The exemption is by **exact path** — not a prefix, which
would also exempt a hypothetical `model-routing-notes.md`, and not a line range,
which is the shape `docs/evidence/34-...md` recorded a mutation surviving inside.

## Mutation testing

Against the **committed** tree at `6d57de7`. `git status --porcelain` empty
before and after each.

| # | Mutation | Shipped toolchain | Verdict |
|---|---|---|---|
| M1 | Reintroduce a retired allow-list entry | `allowlist-stale` 1 violation **and** `path-refs.test.ts` 60/3 | **KILLED**, over-determined |
| M2 | Put a literal model ID in a **non-exempt** reference | **entirely green** — test 0 fail, lint 0/91, skill-lint 0, claim-lint 88/0 | **SURVIVED** |
| M3 | Restore the literal floor at the real 29-entry state | 62/1 | **KILLED** — #36's forward-dated kill, landed |
| M4 | Corrupt a `:NN` citation to a **real but wrong** line | **entirely green** | **SURVIVED** |

### M2 — `CLAIM-35.3` joins the unenforced list

A literal model ID placed in `references/data-classification.md` passes the whole
chain. The claim this Story's Design spent Problem 1 and Decision 1 on is, today,
checked by nobody.

**Three of S2.1's claims are now measured as unenforced**, not merely noted:

| Claim | Proven unenforced by |
|---|---|
| `NEVER-35.7` — no restatement | #37 M2 |
| `CLAIM-35.4` case 10 — no frontmatter | #37 M3 |
| `CLAIM-35.3` — model IDs confined | **#38 M2** |

All three land on #280, which is sequenced last so its denominators are real
rather than zero. **Four more documents (#39, #40) will be authored under the
same absence.**

### M4 — finding 32, demonstrated rather than argued

The citation `packages/core/src/classify/levels.ts:5` was changed to `:9`. Line 9
exists and reads `INTERNAL: 1,` — a real line with entirely the wrong content.

**Nothing detected it.** `path-dangling` asks whether
`packages/core/src/classify/levels.ts` exists; it does.

This is the class that has now produced **six** instances during S2.1, one of them
in shipped code (`packages/core/src/guards/commit-prefix.ts:3-5` → a blank line),
and **two of them written by me in #38 while actively watching for it**.

> **Diligence has been tried and has failed six times in one Story.** The control
> has to be a guard: resolve the `:NN` and assert the line still contains what the
> citation claims. Nobody has designed one, and **no issue has been raised.**

## Case results

| Case | Claim | Result | Basis |
|---|---|---|---|
| 1 | `CLAIM-35.1` | **PARTIAL — 8/12** | Cumulative. 12/12 is Story-level, reachable at #40 |
| 2 | `CLAIM-35.1` | **DEFERRED** | Four entries remain allow-listed, correctly |
| 6 | `CLAIM-35.3` | **PASS by measurement, UNENFORCED** | 7 hits in the exempt file, 0 across seven others. M2 proves nothing would catch a regression |
| 7 | `CLAIM-35.3` | **PASS by inspection** | Exemption is exact-path, not prefix or range. No rule exists to test it against yet |
| 10 | `CLAIM-35.4` | **PASS by measurement, UNENFORCED** | Both documents lack frontmatter |
| 14 | `CLAIM-35.6` | **PASS (partial scope)** | Reconciliation table per document; four rows carried with named owners |
| 15 | `NEVER-35.7` | **PASS over a denominator of 5** | 0 restatements |
| 17 | `NEVER-35.8` | **PASS** | `allowlist-stale` 0; 31 → 29; family 6 → 4 |
| 18 | `NEVER-35.8` | **PASS — and this is the case's real test** | 63/0 at 29 entries, where the pre-#36 assertion fails |
| 19 | `NEVER-35.8` | **PASS** | Mutation M1 |

## Findings

| # | Finding | Disposition |
|---|---|---|
| 1 | **Two stale `:NN` citations were authored in this task** — `02-roles.md:556` (blank; rule at `:555-560`) and `09-security.md:53` (blank; table at `:47-52`) | **Both fixed before commit**, caught by resolving every citation. Self-inflicted while documenting the hazard |
| 2 | `CLAIM-35.3` is unenforced (M2) | **Carried to #280.** Now three claims, all measured |
| 3 | The `:NN` class needs a guard, not diligence (M4) | **No issue raised.** Raising one is a scope decision |
| 4 | `packages/core/src/routing` is recorded as milestone `M2` in the allow-list; it is an **M3/S3.1** target | **Carried** — recorded in `references/model-routing.md`. Owner M3. `CLAIM-35.3`'s seeded exemption clause is vacuous for all of M2 as a result |

## Known limits of this verdict

- **`skill-lint` scanned 0 files.** Unchanged since S1.3.
- **Three of this Story's claims are verified by measurement and an uncommitted
  script.** Point-in-time. Nothing prevents regression in #39 or #40.
- **These eight documents are rework candidates** if #280's shipped rules prove
  stricter than the checks used here.
- Numbers are measured at `6d57de7`. Committing this artifact takes `expect()`
  from 2579 to **2581**, because a case reads `docs/evidence/` at run time.
