---
issue: 41
repo: dev-pmallapp/iAI
branch: story/41-s2-2-planning-skills
commit: da0099dc1e1f8e2125d15be1aad3474af6c5a1b4
run: 2026-09-07T04:04:01Z
verdict: PASS
---

# Evidence: S2.2 — Planning skills

**Story-level roll-up. Nine of nine tasks closed, re-derived at `da0099d`.**

| Field | Value |
|-------|-------|
| Verdict | **PASS**, with three claims explicitly **not** discharged — see the claim table |
| Chain | **903 tests / 889 pass / 0 fail / 14 skip / 3158 `expect()`** · lint **0** across 95 files · typecheck **0** · skill-lint **4 `SKILL.md`, 17 rules, 0 errors, 0 warnings** · claim-lint **111 files, 0 errors, 1 warning** |
| Denominators | `testplan-corpus` 9 plans / 49 case tables / 220 cases · `design-spine` 10 Designs / 103 sections · claim-lint scopes: docs 90, scripts 11, .github 1, **skills 5**, root 4 |
| Commits | 31 on the story branch since `main` |
| Tasks | #46, #294, #295, #296, #44, #42, #43, #45, #297 — all `status:resolved`, all merged |
| Raised | **#301, #304, #305, #307** — plus #292, #293, #299 carried from the gate |

**The 1 warning is `docs/design/stories/9.md` and must remain a warning.** Gate
ruling G1b is "recorded rather than failed"; a mutation promoting it to an error
kills four cases.

## What S2.2 shipped

**Four Tier-1 planning verbs**, the first `SKILL.md` files in the repository:

| Skill | Hard-fails on | Reads a domain | Writes |
|---|---|---|---|
| `goal-create` | an unresolvable goal | **no** — named exemption, #307 | one milestone + feature table |
| `story-create` | no resolvable `domain:` in the milestone document | yes, from the Story block's metadata line | one Story per uncovered row, plus the orphan report |
| `story-design` | no `domain:` label on the Story | yes | the Design; spine cited, not copied |
| `story-test-plan` | no label, or no Design carrying a claim | yes | the plan; every case declaring a `Corpus` |

**Three new lint rules** — `domain-routing-form` (#46), `phase-0-section` and
`error-handling-section` (#295) — taking `skill-lint` from 14 ids to **17**.

**Two linters made self-describing.** `skill-lint` exports `RULE_IDS`;
`claim-lint` exports `SCOPE_DIRS` and prints a **per-scope denominator**.
`CONTRIBUTING.md` now carries a rule table and a scope table, each asserted
against the linter **in both directions**.

**`skills/` entered `claim-lint`'s scope** (#296), and
`references/design-format.md` was reconciled to the gate ruling (#297).

## Claim-by-claim

| Claim | Status | Basis |
|---|---|---|
| `CLAIM-41.1` | **stated, not executed** | `goal-create` declares milestone identity and re-entry; proving a second run mutates nothing needs **#293** |
| `CLAIM-41.2` | **stated, not executed** | same; the orphan report is specified in the body |
| `CLAIM-41.3` | **partial** | the Design-side rule ships and is enforced (`design-spine`, 10 Designs / 103 sections); the skill *writing* one is unexecuted — **#293** |
| `CLAIM-41.4` | **stated, not executed** | the three obligations are specified; `testplan-corpus` enforces the `Corpus` half over 220 cases |
| `CLAIM-41.5` | **DEFECTIVE — unruled** | two of the four skills cannot satisfy it as worded. **#307** |
| `CLAIM-41.6` | **discharged** | `domain-routing-form`, id list imported, 0 violations over 4 skills + 12 contract files |
| `NEVER-41.7` | **discharged** | proved over the **real four-skill corpus**, the denominator the claim names |
| `CLAIM-41.8` | **model-judged** | each body declares a re-entry condition and orders reads before writes; execution needs **#293** |
| `NEVER-41.9` | **discharged** | `skill-lint` scans 4, asserted non-zero **and** equal to the verb-directory count |
| `CLAIM-41.10` | **discharged** | bidirectional set equality, both linters, plus row/scope counts |

**Three claims rest on #293's execution harness**, which the gate ruled a Story
of its own (G4, size L). That is a stated bound, not a gap discovered late:
every skill body in this Story is a **specification**, and nothing here runs one.

## Six plan amendments, all found by implementing

| # | Amendment | Task |
|---|---|---|
| 1 | cases 2, 8, 9, 16 move `skill-lint` → `claim-lint` | #294 |
| 2 | case 2's literal "9 of 9" becomes a run-time count | #294 |
| 3 | case 14's unit moves from the file to the sentence | #46 |
| 4 | case 23 strengthened to **set equality, both directions** | #295 |
| 5 | case 24's tool moves `claim-lint` → `bun test`; per-scope denominator added | #296 |
| 6 | #297's line-neutral edit invalidated citations it could not fix | #297 |

**Not one was found by review.** Amendments 1, 3, 5 and the `CLAIM-41.5` defect
are **four occurrences of one category error** — a rule pointed at a population
that does not have the property it describes — and the fourth landed **inside a
gate-approved CLAIM**.

## What mutation testing found, ten tasks running

| Task | Run | Survived |
|---|---|---|
| #46 | 8 | **M7** — an exemption as a bare prefix |
| #294 | 9 | — |
| #295 | 8 | **M8** — a missing word boundary |
| #296 | 5 | — (but **N2 is killed by exactly one assertion**) |
| #44 | 5 | — |
| #42 | 4 | — |
| #43 | 2 | — |
| #45 | 2 | — (**J2 killed twice**) |
| #297 | — | line-neutrality verified numerically |

**M8 is the finding of the Story.** Two rules written in one sitting to the same
specification, and only one had its boundary asserted: deleting
`error-handling-section`'s `\b` left the entire suite green while
`## Error Handlingz` satisfied the rule. Its sibling's `(?![0-9])` **was**
pinned. The asymmetry — not the missing character — is the lesson.

**N2 and J2 are the other two worth carrying.** N2 left `"skills"` in
`SCOPE_DIRS` while never walking it: four of five assertions passed. J2 deleted
a verb directory: every "zero violations over the real corpus" loop stayed green,
because they iterate over whatever exists. **Both were caught only by an
assertion that reads a denominator, not a result.**

## Three defects caught by review that no mutation would have found

1. **#295** — a repair flipped 12 test call sites to `{ isSkill: false }`,
   silently moving the standing evidence for `CLAIM-41.6`/`NEVER-41.7` off the
   skill population those claims are *about*. The tests passed. They tested the
   wrong population.
2. **#44** — a case-12 matcher banned every domain id as a bare word in a
   Tier-1 body. It passed, and **contradicted gate ruling G2**:
   `references/domain-binding.md:13` states the doctrine these skills implement
   using **3 of the 5 banned ids in one sentence**. It would have blocked #42,
   #43 and #45.
3. **#42** — the exemption that defect forced was written **named, counted and
   asserted** rather than derived, because a derived exemption is the silent
   direction. J2 later proved the guard earns its place.

> Each of the three was a test that **passed** and was **wrong**. Mutation
> testing cannot find those: it perturbs the source and asks whether a test
> notices. It never asks whether the test is asking the right question.

## The stale-`:NN` class, and the eighth request

Findings 30, 32, 39, 40, 41 asked for a guard. **#304 is now open** — the first
time the request has an owner.

#297 is the argument for its scope. That edit was **exactly line-neutral** —
14 lines for 14, 1 for 1, both files unchanged in length — precisely so that
`:31`, `:52` and `:145` would keep resolving. **They do. They are also no longer
true.** `:31` said *"four keys"* and now says there is no frontmatter, so
`stories/41.md:35`, case 17, and **`docs/evidence/294-*.md:57` — immutable** —
all cite a live line that contradicts them.

> **A guard checking that a `:NN` still resolves would have passed #297
> unchanged, because making it pass was the entire design goal.** It has to
> check what the line *says*.

## #299, eight confirmations

Every artifact in this Story predicted its own `expect()` total before landing
and was re-derived exactly: #46 (2978→2980), #295 (3062→3064), #296
(3091→3093), #44 (3112→3114), #42 (3126→3128), #43 (3126→3128), #45
(3154→3156), #297 (3156→3158). **Eight predictions, eight exact.** The issue is
decidable.

## Findings carried forward

- **Build Target 8's premise is false.** It calls for retiring *"four
  `planned`/`M2` entries for the verb directories"*. **They do not exist and
  never did** — the only `M2` entry is `packages/core/src/routing`.
  `allowlist-stale` reported 0 before and after all four skills were created.
- **`CONTRIBUTING.md:147`** still names `packages/installer/src/cli.ts`, which
  does not exist, and `:286-287` tells a new skill's author to run it. **S2.2 is
  the first Story that authored one.** Owner: M8/S8.1.
- **#305** — `DESIGN_SPINE` remains unprotected by `duplicate-contract`. #297
  removed the most likely place a copy would appear, lowering the risk without
  closing the gap.

## Verdict

**PASS.** Nine tasks, four skills, three new rules, two linters made
self-describing, six recorded amendments, four issues raised.

**The Story must not be closed until #307 is ruled.** `CLAIM-41.5` is
gate-approved and two of its four subjects cannot satisfy it as worded; the
named exemption in `test/skill-lint.test.ts` holds that gap open honestly rather
than hiding it, but it is a gap in a registered claim, and only a human can
restate one.
