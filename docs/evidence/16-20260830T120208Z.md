---
issue: 16
story: 15
repo: dev-pmallapp/iAI
commit: 2a044a46d69d92e1f7e40981badf1d941805e4f4
branch: task/16-four-level-classifier
run: 2026-08-30T12:02:08Z
verdict: PASS
---

# Evidence: #16 — four-level data classifier

| Field | Value |
|-------|-------|
| Task | #16 |
| Story | #15 |
| Claims | CLAIM-15.1, CLAIM-15.2 |
| Cases | 1, 2, 13, 14, 17, 18, 19, 20 |
| Verdict | **PASS** — 5/5 P0, 3/3 P1 |

## Case results

Each case number is carried in the `describe` block title, so a filtered run
proves the case exists rather than merely that the file passes.

| # | Priority | Verifier | Tests | Result | How observed |
|---|----------|----------|-------|--------|--------------|
| 1 | P0 | tool-checked | 1 | PASS | 400 public fields + 1 biomarker returns `PRIVATE` for the whole payload |
| 2 | P0 | tool-checked | 4 | PASS | one fixture per level, each returning its own level |
| 13 | P0 | tool-checked | 2 | PASS | unrecognised key and bare unrecognised value both `PRIVATE` |
| 14 | P0 | tool-checked | 7 | PASS | `USER/` corpus, 0 results at `PUBLIC` or `INTERNAL` |
| 17 | P1 | tool-checked | 3 | PASS | single elevated field at `INTERNAL`, `PRIVATE`, `SECRET` each dominates |
| 18 | P0 | tool-checked | 4 | PASS | `{}`, `[]`, `null`, `undefined` all `PRIVATE`, none threw |
| 19 | P1 | tool-checked | 4 | PASS | biomarker at depth 10; 10,000 levels does not crash |
| 20 | P1 | tool-checked | 9 | PASS | both must-match and must-not-match corpora |

## Filtered runs, per case

```
case 1  -> 1 pass 0 fail      case 17 -> 3 pass 0 fail
case 2  -> 4 pass 0 fail      case 18 -> 4 pass 0 fail
case 13 -> 2 pass 0 fail      case 19 -> 4 pass 0 fail
case 14 -> 7 pass 0 fail      case 20 -> 9 pass 0 fail
```

34 of the 52 tests in the file are case-attributed. The remaining 18 are
hostile-input and recogniser-table tests carrying no case number.

## Independent probe, run outside the test suite

The implementing agent's report is a claim. These were run directly against the
module, not through its own tests:

```
400 public + 1 biomarker    -> PRIVATE
{} [] null undefined        -> PRIVATE PRIVATE PRIVATE PRIVATE
circular reference          -> PRIVATE (terminated)
throwing getter             -> PRIVATE (did not propagate)
nesting at 50,000 levels    -> PRIVATE (no stack overflow)
USER/x ./USER/x USER/       -> PRIVATE (4/4 must-match)
USERS/x user/x docs/USER/x  -> INTERNAL (4/4 correctly not matched)
ticker                      -> PUBLIC
```

The 50,000-level probe exceeds the 10,000 the suite covers, and confirms the
explicit stack rather than recursion.

## Full suite

```
 0 fail
 520 expect() calls
Ran 276 tests across 10 files. [3.95s]
```

## Notes

The recogniser tables are closed token sets transcribed from
`docs/design/09-security.md:49-52`, checked SECRET before PRIVATE before
INTERNAL before PUBLIC so any overlap resolves toward the more sensitive class.
Value-shape matching escalates a secret found under an innocent key name.

`classifyPath` is pure string matching. CLAIM-15.6 forbids the I/O that would
let it resolve a path, so case 20 covers both a false negative, which leaks, and
a false positive, which makes the system unusable.
