---
issue: 246
story: 243
repo: dev-pmallapp/iAI
commit: 15540865001bb9b1cbcbf3514707e724810ae026
branch: task/246-retire-permissive-posture
run: 2026-09-01T02:13:47Z
verdict: PASS
---

# Evidence: #246 — retire the permissive posture from the design tree

| Field | Value |
|-------|-------|
| Task | #246 |
| Story | #243 |
| Claims | CLAIM-243.1, CLAIM-243.3, NEVER-243.7 |
| Cases | 1, 2, 6, 7, 15 |
| Verdict | **PASS** — 2/2 P0, 3/3 P1 — with one finding, recorded below |

## Case results

| # | Priority | Claim | Verifier | Result |
|---|----------|-------|----------|--------|
| 1 | P0 | CLAIM-243.1 | tool-checked | PASS with a finding — see below |
| 2 | P1 | CLAIM-243.1 | human-attested | PASS — the strict posture is stated positively in both the class table and the absolute |
| 6 | P1 | CLAIM-243.3 | human-attested | PASS — `CLAIM-15.4` annotated, names #243, original text intact |
| 7 | P1 | CLAIM-243.3 | human-attested | PASS — `CLAIM-126.3` and `CLAIM-126.4` corrected at source, each naming #243 |
| 15 | P0 | NEVER-243.7 | tool-checked | PASS — 0 files changed under `docs/evidence/` |

## Verification chain

```
build       0
test        425 pass / 0 fail / 6 skip
lint        0 violations across 43 files
typecheck   0
skill-lint  0 files, 0 errors
claim-lint  55 files, 0 errors
```

## Case 15 — evidence immutability

```
$ git diff --name-only main...HEAD -- docs/evidence
(empty)
```

No posted `## iai-evidence` or `## iai-verdict` comment was edited. #15's verdict
records a PASS against the matrix as it stood and stays as posted. Forward-only.

## Case 2 — the strict posture stated positively

`docs/design/09-security.md` was the primary carrier of the permissive reading.
Its `PRIVATE` row now answers the table's own question — *"May reach a cloud
model?"* — with **"Never. Under any condition."**, and the second absolute reads
**"PRIVATE never reaches a cloud model. No opt-in, no consent value, no
de-identified form."**

Both state the rule rather than leaving it to be inferred from the removal of the
permissive one, which is what case 2 exists to check. The absolute also records
that consent is retained but inert, so a reader who meets the parameter in code
has already met the reason it does nothing.

## Case 6 — annotated, not rewritten

`CLAIM-15.4` keeps its original wording. A `> **SUPERSEDED by #243**` block sits
beneath it, and a closing addendum records that @dev-pmallapp ruled on #235 on
2026-08-31 and that #243 carries the ruling out.

This is Decision 4 working as intended: `docs/evidence/15-*.md` records a PASS
taken against the claim as written, so rewriting the claim would leave the
evidence asserting something the Design no longer says.

## Case 1 — the search, and what it turned up

Searched `docs/**/*.md` and root markdown for `opt-in`, `de-identified`,
`deidentified` and `per-session`.

Hits fall into five groups, four of them expected:

| Group | Verdict |
|---|---|
| Unrelated senses of "opt-in" — trading rungs, permitted instruments | Not about PRIVATE egress |
| Statements of the **strict** posture | These are the fix, not the defect |
| Lines carrying an explicit superseded marker — `M1.md:94`, `M5.md:211-214`, `15-plan.md`'s *Superseded cases* section | Sanctioned by Decision 4 |
| #243's own Design and test plan describing the cell being removed | Cannot state the removal without naming it |
| `docs/design/verification-pass.md`, `docs/evidence/**` | Permanently excluded per the four-path rule |

### The finding: `docs/design/stories/15.md` needs to be the fifth excluded path

Note 2 of `docs/test-plans/243-plan.md` anticipates the permissive phrasing
surviving *"in exactly one place"* — the `CLAIM-15.4` annotation — and states
that **any further exclusion is a finding rather than a search adjustment.**

It survives in more than one place. Beyond the annotated claim, `stories/15.md`
still carries the permissive reading in its Decision 3 matrix rationale
(`:245`), the whole of Decision 4 (`:291-305`), and Decision 5's contrast between
`PRIVATE` and `SECRET` (`:360`, `:369`).

**None of these was rewritten, and none should be.** They are the sealed
historical record of what #15 decided and why, and the Story's own Constraints
forbid editing a closed Design such that its evidence no longer matches. The
honest conclusion is that `docs/design/stories/15.md` belongs on the permanent
exclusion list alongside `stories/194.md`, `194-plan.md`, `verification-pass.md`
and `docs/evidence/**` — it is another instance of the pattern named in the
handoff: *a document whose subject is the migration must be excluded from the
migration.* That would make it the sixth exclusion and the seventh occurrence of
the pattern.

**Recorded here rather than acted on.** Adding a fifth entry to the exclusion
list is a decision for the human principal, and no exclusion list was silently
widened to make this case pass.

### One gap closed during verification

Verification found `CLAIM-15.4`'s **Test Strategy table row** (`:184`) still
reading *"with opt-in returns `allow` + de-identified `redacted`"* with no
superseded marker. The claim and the addendum were annotated; the row was not.

That is materially different from the narrative prose above it. A table is the
surface a reader consults for current behaviour, and this one sat two sections
above an addendum it never pointed at. Annotated in `1554086`, using the same
pattern Decision 4 already sanctioned — a note beside the row, with the row's own
text untouched.

The remaining prose is left alone deliberately: it reads as historical argument
(*"The tree holds two irreconcilable positions…"*), not as current policy.

<sub>Written by iAI, hand-executed. `/iai:task-verify` lands in M2.</sub>
